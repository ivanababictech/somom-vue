import ApprovalQuestions from './main';

let parent = 'corporate'

export default [
    {
        path: `${parent}/approval-questions`,
        name: `${parent}.approval-questions`,
        component: ApprovalQuestions
    }
]