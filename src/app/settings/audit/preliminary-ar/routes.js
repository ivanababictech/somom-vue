import auditAccountingSection from './main'

let parent = 'global'

export default [
    {
        path: parent + '/audit-accounting-section',
        name: parent + '.audit-accounting-section',
        component: auditAccountingSection
    },
]
